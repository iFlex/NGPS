iFrame: Links are open in parent...
google does not load because of same origin

quickAddInterface:
add camera does not work for factory.root

appChoice & factory:
removeGlobalApp does not work in unsafe mode:
can't properly remove the dom references of the host

linkEdit & container
need to implement multipart link
